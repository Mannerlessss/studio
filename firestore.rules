
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules are insecure and are open to public access.
    // These are for development purposes only to bypass permission errors.
    // TODO: Implement secure rules before deploying to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
