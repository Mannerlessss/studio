
// Firebase is disconnected.
// To reconnect, you will need to re-initialize the Firebase app
// and re-establish the auth and db connections.

const app = null;
const auth = null;
const db = null;

export { app, auth, db };
